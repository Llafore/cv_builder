<header>

    <div class="alias">
        <h1>{{name}}</h1>
        <strong>{{position}}</strong>
    </div>

    <div class="contact">
        {{#each contacts}}
            <span>{{this}}</span>
        {{/each}}
    </div>

</header>